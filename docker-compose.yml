version: '3.8'

services:
  oracle-xe:
    image: gvenzl/oracle-xe:21-slim
    container_name: oracle-xe
    ports:
      - "1521:1521"
    environment:
      ORACLE_PASSWORD: Sifreniz123
    volumes:
      - oracle-data:/opt/oracle/oradata

volumes:
  oracle-data: